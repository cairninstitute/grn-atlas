---
name: grn-celltype-regulon
description: "Extract a TF's regulon filtered by cell-type or state expression context. Shows which targets are active in a specific cell state vs the global regulon, with overlap statistics."
compatibility: Requires the grn-atlas backend virtualenv (backend/venv/bin/python) or a running GRN Atlas server. Run `make setup` to create the venv.
metadata:
  author: grn-atlas
  version: "1.0"
---
