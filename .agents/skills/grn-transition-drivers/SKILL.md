---
name: grn-transition-drivers
description: "Identify TF drivers of a cell-state transition or developmental branch point. Accepts transition DEGs and uses regulon enrichment to find the TFs most likely driving the change."
compatibility: Requires the grn-atlas backend virtualenv (backend/venv/bin/python) or a running GRN Atlas server. Run `make setup` to create the venv.
metadata:
  author: grn-atlas
  version: "1.0"
---
